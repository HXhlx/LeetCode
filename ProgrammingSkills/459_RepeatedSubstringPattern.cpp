#include <bits/extc++.h>
#include "gtest/gtest.h"
using namespace std;
class Solution
{
public:
    bool repeatedSubstringPattern(string s)
    {
        string rsp;
        for (wint_t i = 0, n = s.length(); i < n; ++i)
        {
        }
    }
};